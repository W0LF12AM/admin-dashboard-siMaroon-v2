import 'package:flutter/material.dart';

class Kelas extends StatelessWidget {
  const Kelas({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(child: Center(child: Text('Kelas')));
  }
}
