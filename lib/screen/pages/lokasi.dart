import 'package:flutter/material.dart';

class Lokasi extends StatelessWidget {
  const Lokasi({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(child: Center(child: Text('Lokasi')));
  }
}
